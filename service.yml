apiVersion: v1
kind: Service
metadata:
  name: python-app-svc
spec:
  type: NodePort
  selector:
    app: python-app
  ports:
      port: 80
      targetPort: 8000 # Application is running on 8000 port
      nodePort: 30007 # you can give by your choice
